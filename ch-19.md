# 模式与模式匹配

模式由字面值、解构、变量、通配符、占位符等组成，用于匹配数据结构。

## 可使用模式的位置
```rs
// match 分支
match x {
    Some(v) => v,
    None => 0,
}

// if let
if let Some(v) = x { println!("{v}"); }

// let 语句（不可反驳模式）
let (x, y, z) = (1, 2, 3);

// while let
while let Some(top) = stack.pop() { println!("{top}"); }

// for 循环
for (index, value) in v.iter().enumerate() { println!("{index}: {value}"); }

// 函数参数
fn print_coordinates(&(x, y): &(i32, i32)) { println!("({x}, {y})"); }

// let else —— 模式不匹配时返回/退出
let Some(v) = opt else { return; };
```

## 可反驳与不可反驳
- **不可反驳**（irrefutable）：总能匹配（`let x = 5;`）
- **可反驳**（refutable）：可能不匹配（`if let Some(x) = ...`）
- `let`、`for`、函数参数只能用不可反驳模式
- `if let`、`while let` 用可反驳模式

## 模式语法
```rs
// 字面值
match x { 1 => ..., 2 => ..., _ => ... }

// 命名变量（会遮蔽外部同名变量）
match x { Some(y) => ..., _ => ... }

// 多模式（|）
match x { 1 | 2 => println!("one or two"), _ => () }

// 范围（..=）
match x { 1..=5 => ..., _ => ... }
match c { 'a'..='j' => ..., _ => ... }

// 解构结构体
let p = Point { x: 0, y: 7 };
let Point { x: a, y: b } = p;       // 重命名
let Point { x, y } = p;             // 简写

match p { Point { x, y: 0 } => ..., Point { x: 0, y } => ..., Point { x, y } => ... }

// 解构枚举
match msg {
    Message::Quit => ...,
    Message::Move { x, y } => ...,  // 解构结构体变体
    Message::Write(text) => ...,    // 解构元组变体
    Message::ChangeColor(r, g, b) => ...,
}

// 解构嵌套
let ((feet, inches), Point { x, y }) = ...;

// 忽略值
fn foo(_: i32, y: i32) { ... }          // _ 忽略参数
match s { Some(_) => ..., None => ... }  // _ 忽略值
match s { Some(_x) => ..., None => ... } // _x 绑定但不使用（不警告）
let (a, .., c) = (1, 2, 3, 4, 5);       // .. 忽略剩余
match origin { Point { x, .. } => ... }  // 结构体中忽略

// 匹配守卫（match guard）
let num = Some(4);
match num {
    Some(x) if x % 2 == 0 => println!("even: {x}"),
    Some(x) => println!("odd: {x}"),
    None => (),
}

// @ 绑定：测试值的同时绑定变量
match msg {
    Message::Hello { id: id_variable @ 3..=7 } => println!("id in range: {id_variable}"),
    Message::Hello { id: 10..=12 } => println!("id in another range"),
    Message::Hello { id } => println!("other id: {id}"),
}
```
