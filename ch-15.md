# 智能指针

智能指针是行为类似指针但有额外元数据和功能的数据结构，通常实现了 `Deref` 和 `Drop` trait。

## Box<T> —— 堆上分配
```rs
// 将数据存储在堆上，栈上存指针
let b = Box::new(5);
println!("b = {}", b);  // 自动解引用

// 用于：
// 1. 编译时大小未知的类型（如递归类型）
enum List {
    Cons(i32, Box<List>),
    Nil,
}
use List::{Cons, Nil};
let list = Cons(1, Box::new(Cons(2, Box::new(Cons(3, Box::new(Nil))))));

// 2. 大型数据转移所有权时不复制
// 3. 拥有实现了特定 trait 的类型（trait object）
```

## Deref Trait —— 智能指针当引用用
```rs
use std::ops::Deref;

struct MyBox<T>(T);

impl<T> MyBox<T> {
    fn new(x: T) -> MyBox<T> { MyBox(x) }
}

impl<T> Deref for MyBox<T> {
    type Target = T;
    fn deref(&self) -> &Self::Target { &self.0 }
}

let x = 5;
let y = MyBox::new(x);
assert_eq!(5, *y);  // *y 等价于 *(y.deref())

// 解引用强制多态（deref coercions）：
// 可将 &MyBox<String> 自动转为 &String，再转为 &str
fn hello(name: &str) { println!("Hello, {name}!"); }
let m = MyBox::new(String::from("Rust"));
hello(&m);  // 自动解引用转换
```

## Drop Trait —— 清理代码
```rs
struct CustomSmartPointer { data: String }

impl Drop for CustomSmartPointer {
    fn drop(&mut self) {
        println!("Dropping CustomSmartPointer with data `{}`!", self.data);
    }
}
// 离开作用域时自动调用 drop

// 手动提前释放（不能直接调用 drop 方法，用 std::mem::drop）
let c = CustomSmartPointer { data: String::from("stuff") };
drop(c);  // std::mem::drop，提前释放
```

## Rc<T> —— 引用计数（单线程）
```rs
use std::rc::Rc;

// 用于多所有权场景（如多个链表共享子列表）
enum List { Cons(i32, Rc<List>), Nil }
use List::{Cons, Nil};

let a = Rc::new(Cons(5, Rc::new(Cons(10, Rc::new(Nil)))));
let b = Cons(3, Rc::clone(&a));  // 克隆 Rc，增加引用计数
let c = Cons(4, Rc::clone(&a));
// Rc::strong_count(&a) == 3   查看引用计数
// Rc<T> 只允许不可变引用，单线程使用
```

## RefCell<T> —— 内部可变性
```rs
use std::cell::RefCell;

// 允许在拥有不可变引用时修改内部值（运行时检查借用规则）
let data = RefCell::new(5);
*data.borrow_mut() += 1;      // borrow_mut() 获取可变引用
println!("{}", data.borrow()); // borrow() 获取不可变引用

// 违反规则时会 panic（而非编译错误）
// 同一作用域内不能有两个可变借用

// 常见组合：Rc<RefCell<T>> —— 多所有者的可变数据
let value = Rc::new(RefCell::new(5));
let a = Rc::clone(&value);
let b = Rc::clone(&value);
*b.borrow_mut() += 10;
```

## 引用循环与内存泄漏
```rs
// Rc<T> + RefCell<T> 可能创建引用循环导致内存泄漏
// 解决方案：使用 Weak<T>（弱引用，不影响引用计数）
use std::rc::{Rc, Weak};
use std::cell::RefCell;

// Rc::downgrade() 创建 Weak<T>
// Weak::upgrade() 返回 Option<Rc<T>>
// 使用 Weak 避免循环引用（如树结构中父节点用 Weak 指向子节点）
```
