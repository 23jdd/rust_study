# 无畏并发

Rust 的所有权和类型系统让许多并发错误在编译时被捕获。

## 使用线程
```rs
use std::thread;
use std::time::Duration;

// 创建新线程
let handle = thread::spawn(|| {
    for i in 1..10 {
        println!("hi number {} from the spawned thread!", i);
        thread::sleep(Duration::from_millis(1));
    }
});

// 主线程代码
for i in 1..5 {
    println!("hi number {} from the main thread!", i);
    thread::sleep(Duration::from_millis(1));
}

handle.join().unwrap();  // 等待线程结束

// move 闭包：将所有权转移给线程
let v = vec![1, 2, 3];
let handle = thread::spawn(move || {
    println!("Here's a vector: {:?}", v);
});
// v 已被 move，这里不能再使用
```

## 消息传递（Channel）
```rs
use std::sync::mpsc;  // multiple producer, single consumer
use std::thread;

// 创建信道
let (tx, rx) = mpsc::channel();

// 多个发送者
let tx1 = tx.clone();

thread::spawn(move || {
    let val = String::from("hi");
    tx.send(val).unwrap();  // send 获取所有权
    // val 已被 move，不能再用
});

let received = rx.recv().unwrap();  // 阻塞接收
// rx.try_recv()  // 非阻塞接收，返回 Result

// 迭代接收
for received in rx {
    println!("Got: {}", received);
}
```

## 共享状态并发（Mutex）
```rs
use std::sync::{Arc, Mutex};
use std::thread;

// Mutex 互斥锁：同一时间只允许一个线程访问数据
let m = Mutex::new(5);
{
    let mut num = m.lock().unwrap();  // 获取锁
    *num = 6;  // 修改
}  // 离开作用域自动释放锁

// Arc（原子引用计数）+ Mutex：多线程共享可变状态
let counter = Arc::new(Mutex::new(0));
let mut handles = vec![];

for _ in 0..10 {
    let counter = Arc::clone(&counter);
    let handle = thread::spawn(move || {
        let mut num = counter.lock().unwrap();
        *num += 1;
    });
    handles.push(handle);
}

for handle in handles { handle.join().unwrap(); }
println!("Result: {}", *counter.lock().unwrap());  // 10
```

## Sync 和 Send trait
- `Send`：类型的所有权可以在线程间传递（几乎所有 Rust 类型都是 Send）
- `Sync`：类型的引用可以在线程间共享（类型为 Sync 当且仅当 `&T` 是 Send）
- 这两个是自动推导的标记 trait（`unsafe` trait），标准库类型和大部分自定义类型自动实现
- `Rc<T>` 不是 Send/Sync，`RefCell<T>` 是 Send 但不是 Sync
- `Mutex<T>` 是 Sync，`Arc<T>` 是 Send + Sync（当 T 是 Send + Sync）
