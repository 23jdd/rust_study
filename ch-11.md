# 编写自动化测试

## 如何编写测试
```rs
// 测试函数通常位于 tests 模块中，用 #[cfg(test)] 标注
#[cfg(test)]
mod tests {
    use super::*;  // 引入外部模块的项

    #[test]               // 标注为测试函数
    fn it_works() {
        let result = 2 + 2;
        assert_eq!(result, 4);      // 断言相等
    }

    #[test]
    fn larger_can_hold_smaller() {
        let larger = Rectangle { width: 8, height: 7 };
        let smaller = Rectangle { width: 5, height: 1 };
        assert!(larger.can_hold(&smaller));   // 断言为 true
    }

    #[test]
    fn greeting_contains_name() {
        let result = greeting("Carol");
        assert!(
            result.contains("Carol"),
            "Greeting did not contain name, value was `{}`",  // 自定义错误信息
            result
        );
    }
}

// 断言 panic
#[test]
#[should_panic(expected = "less than or equal to 100")]  // 可选：匹配 panic 信息
fn greater_than_100() {
    Guess::new(200);
}

// 使用 Result<T, E> 编写测试
#[test]
fn it_works_with_result() -> Result<(), String> {
    if 2 + 2 == 4 { Ok(()) }
    else { Err(String::from("two plus two does not equal four")) }
}
```

## 控制测试运行
```bash
cargo test                  # 运行所有测试
cargo test -- --test-threads=1  # 单线程运行
cargo test -- --show-output     # 显示通过的测试的打印输出
cargo test test_name            # 按名称过滤运行单个/部分测试
cargo test -- --ignored         # 只运行被忽略的测试
```

```rs
#[test]
#[ignore]  // 默认跳过此测试
fn expensive_test() { ... }
```

## 测试组织
- **单元测试**：与源码在同一文件，放在 `#[cfg(test)]` 标注的 `tests` 模块中。测试私有函数。
- **集成测试**：放在项目根目录的 `tests/` 目录下，每个文件是独立的 crate。只能测试公开 API。

```rs
// tests/integration_test.rs
use adder;  // 导入库 crate

#[test]
fn it_adds_two() {
    assert_eq!(4, adder::add_two(2));
}
```
