# 附录

## A - 关键字
Rust 保留关键字，部分仅在特定上下文使用。

**严格关键字**：as, async, await, break, const, continue, crate, dyn, else, enum, extern, false, fn, for, if, impl, in, let, loop, match, mod, move, mut, pub, ref, return, self, Self, static, struct, super, trait, true, type, union, unsafe, use, where, while

**保留关键字**（将来可能使用）：abstract, become, do, final, macro, override, priv, try, typeof, unsized, virtual, yield

## B - 运算符与符号
| 运算符 | 说明 |
|--------|------|
| `!` | 宏展开 / 取反 / 解引用 / Never类型 |
| `&` / `&&` | 引用 / 借用 |
| `*` | 解引用 / 乘法 / 通配符 |
| `+` / `-` / `*` / `/` / `%` | 算术运算 |
| `==` / `!=` / `<` / `>` / `<=` / `>=` | 比较 |
| `&&` / `\|\|` | 逻辑与 / 或 |
| `\|` | 位或 / 模式中的多选 |
| `..` / `..=` | 范围 |
| `=` / `+=` / `-=` 等 | 赋值 |
| `=>` | match 分支 / 闭包 |
| `?` | 错误传播 |
| `#` | 属性 |
| `::` | 路径分隔符 |
| `<-` | 已移除（曾是 move 的语法） |

## C - 可派生的 trait
可通过 `#[derive]` 自动实现的 trait：

- `Debug`：调试输出 `{:?}`
- `Clone`：克隆值
- `Copy`：按位复制（要求类型实现 Copy）
- `PartialEq`：相等比较 `==`
- `Eq`：等价关系
- `PartialOrd`：部分排序 `<`
- `Ord`：全序排列
- `Hash`：哈希
- `Default`：默认值

## D - 实用开发工具
- **rustfmt**：代码格式化工具 `cargo fmt`
- **rustfix**：自动修复编译器警告 `cargo fix`
- **Clippy**：代码风格检查 `cargo clippy`
- **rust-analyzer**：IDE 支持（VS Code、IntelliJ 等）
- **Rust 文档**：`rustup doc` 打开本地文档

## E - 版本（Edition）
- **2015**：最初稳定化的语法
- **2018**：重大改进，引入 `dyn`、`impl Trait`等
- **2021**：更严格的 borrow checker，闭包改进等
- **2024**：reserve 语法（gen），`unsafe` 改进等
```toml
[package]
edition = "2024"
```

## F - 译本
本书有多个社区翻译版本。英文原版：https://doc.rust-lang.org/stable/book/

## G - Rust 的开发与 "Nightly Rust"
- **Stable**：稳定版，大多数用户使用
- **Beta**：预览版，即将稳定
- **Nightly**：每日构建，最新特性（需要不稳定特性标志 `#![feature(...)]`）
```bash
rustup default stable    # 切换到稳定版
rustup default nightly   # 切换到 nightly
rustup update            # 更新
```
