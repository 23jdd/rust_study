# 函数式语言特性：迭代器与闭包

## 闭包（Closures）
闭包是可以捕获其环境的匿名函数。

```rs
// 基本语法
let expensive_closure = |num: u32| -> u32 {
    println!("calculating slowly...");
    std::thread::sleep(std::time::Duration::from_secs(2));
    num
};

// 闭包可省略类型注解
let add_one = |x| x + 1;
let add_one = |x| { x + 1 };

// 闭包可以从环境中捕获值
let mut list = vec![1, 2, 3];
let mut borrows_mutably = || list.push(7);  // 可变借用

// 三种捕获方式（由编译器自动推断，按需选择优先顺序）：
// 1. 不可变借用 (&T)
// 2. 可变借用 (&mut T)
// 3. 获取所有权 (T) —— 使用 move 关键字强制
let list = vec![1, 2, 3];
std::thread::spawn(move || println!("From thread: {:?}", list)).join().unwrap();

// Fn trait（闭包自动实现其中之一）：
// FnOnce：获取所有权，只能调用一次
// FnMut：可变借用，可多次调用
// Fn：不可变借用，可多次调用
```

## 迭代器（Iterators）
迭代器模式允许对一系列元素执行操作。

```rs
let v1 = vec![1, 2, 3];
let v1_iter = v1.iter();  // 不可变引用迭代器
// v1.into_iter()  获取所有权
// v1.iter_mut()   可变引用迭代器

// 消耗迭代器的方法
for val in v1_iter { println!("Got: {}", val); }

// 迭代器适配器（惰性，需要消耗方法才执行）
let v2: Vec<_> = v1.iter().map(|x| x + 1).collect();        // [2, 3, 4]
let shoes: Vec<_> = shoes.into_iter().filter(|s| s.size == shoe_size).collect();

// 消费适配器
let total: i32 = v1.iter().sum();    // 求和
let count = v1.iter().count();       // 计数
let last = v1.iter().last();         // 最后一个元素

// 创建迭代器
// 1..5        // Range
// (1..=5)     // RangeInclusive
// vec.iter()
// iter::repeat(4)   // 无限重复
```

## 改进 I/O 项目
```rs
// 使用迭代器替代索引访问 args
impl Config {
    pub fn build(mut args: impl Iterator<Item = String>) -> Result<Config, &'static str> {
        args.next();  // 跳过程序名
        let query = args.next().ok_or("Didn't get a query string")?;
        let file_path = args.next().ok_or("Didn't get a file path")?;
        Ok(Config { query, file_path, ignore_case: env::var("IGNORE_CASE").is_ok() })
    }
}

// 使用迭代器改写 search（更函数式）
pub fn search<'a>(query: &str, contents: &'a str) -> Vec<&'a str> {
    contents.lines().filter(|line| line.contains(query)).collect()
}
```

## 性能
Rust 的迭代器是**零成本抽象**（zero-cost abstraction），编译后与手写循环性能相当甚至更好。
