# 异步编程基础：Async、Await、Future 与 Stream

异步编程让我们可以用"可能暂停的点"和"最终结果"来表达代码。

## Future 和 async 语法
```rs
// async 关键字创建 Future
async fn do_something() -> u32 { 42 }

// async 块
let future = async {
    let result = do_something().await;  // .await 等待 Future 完成
    println!("result: {result}");
};

// 需要运行时来执行 Future（如 tokio）
// Cargo.toml:
// [dependencies]
// tokio = { version = "1", features = ["full"] }

#[tokio::main]
async fn main() {
    let result = do_something().await;
    println!("{result}");
}
```

## 并发与 async
```rs
use tokio::time::{sleep, Duration};

async fn task(name: &str, secs: u64) {
    println!("{name} 开始");
    sleep(Duration::from_secs(secs)).await;
    println!("{name} 完成");
}

#[tokio::main]
async fn main() {
    // 并发执行（两个任务交替进行）
    tokio::join!(
        task("A", 2),
        task("B", 1),
    );
    // 总共约 2 秒，而非 3 秒
}
```

## 连接并发（join! vs select!）
```rs
// join! 等待所有 Future 完成
let (a, b) = tokio::join!(future_a, future_b);

// select! 等待任意一个 Future 先完成
tokio::select! {
    result = future_a() => { println!("A 先完成: {result}"); }
    result = future_b() => { println!("B 先完成: {result}"); }
}
```

## Stream：按顺序出现的 Future
```rs
use tokio_stream::StreamExt;

// Stream 是一系列异步值（类似 Iterator 的异步版本）
async fn process_stream(mut stream: impl StreamExt<Item = u32> + Unpin) {
    while let Some(value) = stream.next().await {
        println!("收到: {value}");
    }
}
```

## 异步 vs 多线程
| 场景 | 推荐 |
|------|------|
| CPU 密集型 | 多线程 |
| I/O 密集型 | 异步 |
| 大量并发连接 | 异步 |
| 需要并行计算 | 多线程 |
| 混合场景 | 组合使用 |

```rs
// 组合异步 + 线程：使用 spawn_blocking
let result = tokio::task::spawn_blocking(|| {
    // CPU 密集型工作
    (0..1_000_000).sum::<u64>()
}).await.unwrap();
```
