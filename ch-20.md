# 高级特性

## 不安全 Rust（unsafe）
```rs
// unsafe 块允许五种操作（其他安全检查依然生效）：
// 1. 解引用裸指针
let mut num = 5;
let r1 = &num as *const i32;   // 不可变裸指针
let r2 = &mut num as *mut i32; // 可变裸指针
unsafe {
    println!("r1 is: {}", *r1);
    *r2 += 1;
}

// 2. 调用 unsafe 函数或方法
unsafe fn dangerous() { /* ... */ }
unsafe { dangerous(); }

// 3. 访问或修改可变静态变量
static mut COUNTER: u32 = 0;
unsafe { COUNTER += 1; }

// 4. 实现 unsafe trait
unsafe trait Foo { /* ... */ }
unsafe impl Foo for i32 { /* ... */ }

// 5. 访问 union 的字段
// 建议将 unsafe 代码封装在安全的抽象中
```

## 高级 Trait
```rs
// 关联类型（Associated Types）
pub trait Iterator {
    type Item;  // 关联类型：占位符
    fn next(&mut self) -> Option<Self::Item>;
}

// 默认泛型类型参数
trait Add<Rhs=Self> {  // Rhs 默认是 Self
    type Output;
    fn add(self, rhs: Rhs) -> Self::Output;
}

// 完全限定语法（Fully Qualified Syntax）
// <Type as Trait>::function(receiver_if_method, next_arg, ...);
<Dog as Animal>::baby_name();

// 超 trait（Supertrait）
trait OutlinePrint: fmt::Display {  // 要求实现 Display
    fn outline_print(&self) { ... }
}

// newtype 模式（为外部类型实现外部 trait）
struct Wrapper(Vec<String>);
impl fmt::Display for Wrapper {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
        write!(f, "[{}]", self.0.join(", "))
    }
}
```

## 高级类型
```rs
// newtype 模式封装（类型别名）
type Kilometers = i32;  // 仅为别名，非新类型
type Thunk = Box<dyn Fn() + Send + 'static>;

// Never 类型 !
// fn bar() -> ! { ... }  表示函数从不返回
// continue、panic!、loop {} 的返回值都是 !

// 动态大小类型（DST）与 Sized trait
// str 是 DST（大小在编译期未知），&str 存储指针+长度
// 泛型默认有 Sized bound: fn generic<T: Sized>(t: &T) { ... }
// 放宽限制: fn generic<T: ?Sized>(t: &T) { ... }
```

## 高级函数与闭包
```rs
// 函数指针（fn 类型，实现了 Fn、FnMut、FnOnce）
fn add_one(x: i32) -> i32 { x + 1 }
fn do_twice(f: fn(i32) -> i32, arg: i32) -> i32 { f(arg) + f(arg) }
let answer = do_twice(add_one, 5);  // 12

// 闭包 vs 函数指针
let list_of_numbers = vec![1, 2, 3];
let list_of_strings: Vec<String> = list_of_numbers.iter().map(|i| i.to_string()).collect();
let list_of_strings: Vec<String> = list_of_numbers.iter().map(ToString::to_string).collect();

// 返回闭包
fn returns_closure() -> Box<dyn Fn(i32) -> i32> {
    Box::new(|x| x + 1)   // 必须用 trait object，因为闭包大小未知
}
```

## 宏（Macros）
```rs
// 声明宏（declarative macros）
macro_rules! vec {
    ( $( $x:expr ),* ) => {
        {
            let mut temp_vec = Vec::new();
            $( temp_vec.push($x); )*
            temp_vec
        }
    };
}

// 过程宏（procedural macros）—— 需要单独的 crate
// 1. 自定义 #[derive] 宏
// 2. 类属性宏（Attribute-like macros）
// 3. 类函数宏（Function-like macros）
```
