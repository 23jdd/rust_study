# 更多关于 Cargo 和 Crates.io 的内容

## 发布配置（Release Profiles）
```toml
# Cargo.toml
[profile.dev]     # 开发模式（cargo build 默认）
opt-level = 0      # 无优化，编译快

[profile.release] # 发布模式（cargo build --release）
opt-level = 3      # 最大优化，编译慢，运行快

# 可覆盖任意配置项
[profile.release]
opt-level = 2  # 优化级别 0-3, "s", "z"
```

## 发布到 Crates.io
```toml
# Cargo.toml
[package]
name = "my_crate"
version = "0.1.0"
edition = "2024"
description = "A description"  # 必须
license = "MIT"                 # 必须

# 文档注释（/// 生成 HTML 文档）
/// 将传入数字加一
///
/// # Examples
/// ```
/// let arg = 5;
/// let answer = my_crate::add_one(arg);
/// assert_eq!(6, answer);
/// ```
pub fn add_one(x: i32) -> i32 { x + 1 }

// cargo doc --open  生成并打开文档
// cargo login <token>  登录 crates.io
// cargo publish  发布
// cargo yank --vers 1.0.1  撤回版本（不会删除，只是防止新依赖）
// cargo yank --vers 1.0.1 --undo  取消撤回
```

## Cargo 工作空间（Workspaces）
```toml
# 根 Cargo.toml
[workspace]
members = ["adder", "add_one"]

# 依赖工作空间内的 crate
# adder/Cargo.toml
[dependencies]
add_one = { path = "../add_one" }
```

## cargo install
```bash
cargo install ripgrep   # 从 crates.io 安装二进制 crate 到 ~/.cargo/bin/
```

## 自定义 Cargo 扩展
```bash
# cargo-<something> 命名的二进制会被当作 cargo 子命令
cargo something  # 实际调用 cargo-something 二进制
```
